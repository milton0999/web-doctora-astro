---
import type { CollectionEntry } from "astro:content";
import SmartImage from "./SmartImage.astro";
import WhatsAppButton from "./WhatsAppButton.astro";
import { getCollection } from "astro:content";

interface Props {
  servicio: CollectionEntry<"servicios">;
  showPrice?: boolean;
}

const { servicio, showPrice = true } = Astro.props;
const settings = await getCollection('settings');
const whatsappNumber = settings[0]?.data.whatsapp_number || '528115883775';
---

<div class="servicio-card {servicio.data.featured ? 'featured' : ''} mb-4">
  <div class="card h-100 shadow-sm border-0">
    <div class="card-image-container position-relative">
