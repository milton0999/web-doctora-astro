---
// src/layouts/Layout.astro
import { getCollection } from 'astro:content';
import WhatsAppButton from '../components/WhatsAppButton.astro';
const { title, description } = Astro.props;

// Obtener configuración global con manejo de errores
let siteSettings;
try {
  const settings = await getCollection('settings');
  siteSettings = settings[0]?.data || {
    site_name: 'Centro Médico Gonzalitos',
    slogan: 'Cuidando tu salud con la mejor tecnología y atención humana',
    description: 'Ecografías y ultrasonidos de alta calidad en Monterrey',
    logo: null,
    whatsapp_number: '528115883775',
    phone_number: '81 1588 3775',
    email_contact: 'info@centromedicogonzalitos.com',
    address: 'Monterrey, N.L.',
    hours: 'Lun-Vie: 8am-8pm, Sáb: 8am-2pm',
    facebook_url: null,
    instagram_url: null
  };
} catch (error) {
  console.error('Error loading site settings:', error);
  siteSettings = {
    site_name: 'Centro Médico Gonzalitos',
    slogan: 'Cuidando tu salud con la mejor tecnología y atención humana',
    description: 'Ecografías y ultrasonidos de alta calidad en Monterrey',
    logo: null,
    whatsapp_number: '528115883775',
    phone_number: '81 1588 3775',
    email_contact: 'info@centromedicogonzalitos.com',
    address: 'Monterrey, N.L.',
    hours: 'Lun-Vie: 8am-8pm, Sáb: 8am-2pm',
    facebook_url: null,
    instagram_url: null
  };
}

// Obtener colecciones para navegación (optimizado en una sola operación)
let serviciosPorCategoria: { 
  adultos: any[], 
  embarazadas: any[], 
  pediatrico: any[] 
} = { adultos: [], embarazadas: [], pediatrico: [] };
try {
  const servicios = await getCollection('servicios');
  serviciosPorCategoria = {
    adultos: servicios.filter(s => s.data.categoria === 'Adulto'),
    embarazadas: servicios.filter(s => s.data.categoria === 'Embarazada'),
    pediatrico: servicios.filter(s => s.data.categoria === 'Pediatrico')
  };
} catch (error) {
  console.error('Error loading servicios:', error);
}

// Obtener promociones activas
const promotions = await getCollection('promotions');
const activePromotions = promotions.filter(p => 
  p.data.active && new Date(p.data.valid_until) > new Date()
);
---
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title} | {siteSettings.site_name}</title>
    <meta name="description" content={description || siteSettings.description} />
    <meta name="keywords" content="ecografías, ultrasonidos, estudios médicos, monterrey, centro médico, salud" />
    <meta name="author" content={siteSettings.site_name} />
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content={`${title} | ${siteSettings.site_name}`} />
    <meta property="og:description" content={description || siteSettings.description} />
    <meta property="og:image" content={siteSettings.logo || '/img/logo-cmg-horizontal.png'} />
    
    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:title" content={`${title} | ${siteSettings.site_name}`} />
    <meta property="twitter:description" content={description || siteSettings.description} />
    
    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    
    <!-- Meta Tags Geográficos para SEO Local -->
    <meta name="geo.region" content="MX-NLE">
    <meta name="geo.placename" content="Monterrey, Nuevo León">
    <meta name="geo.position" content="25.6714;-100.3090">
    <meta name="ICBM" content="25.6714, -100.3090">
    <meta name="distribution" content="local">
    <meta name="rating" content="General">
    <meta name="revisit-after" content="7 days">
    <meta name="robots" content="index, follow">
    <link rel="canonical" href={`https://centromedicogonzalitos.milcoms.org${Astro.url.pathname}`} />
    
    <!-- Datos Estructurados Schema.org - MedicalClinic -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@graph": [
        {
          "@type": "MedicalClinic",
          "@id": "https://centromedicogonzalitos.milcoms.org/#clinic",
          "name": "Centro Médico Gonzalitos",
          "alternateName": "Clínica de Ultrasonidos Monterrey",
          "description": "Atención médica especializada en ultrasonografía y diagnóstico por imagen en Monterrey",
          "url": "https://centromedicogonzalitos.milcoms.org",
          "telephone": "+528115883775",
          "email": "info@centromedicogonzalitos.com",
          "address": {
            "@type": "PostalAddress",
            "streetAddress": "Avenida Dr. José Eleuterio González 135",
            "addressLocality": "Monterrey",
            "addressRegion": "Nuevo León",
            "postalCode": "64600",
            "addressCountry": "MX",
            "addressLocalityAlternate": "Colonia Leones"
          },
          "geo": {
            "@type": "GeoCoordinates",
            "latitude": "25.6714",
            "longitude": "-100.3090"
          },
          "openingHours": [
            "Mo-Fr 08:00-20:00",
            "Sa 08:00-14:00",
            "Su Closed"
          ],
          "medicalSpecialty": "Radiology",
          "availableService": [
            {
              "@type": "MedicalTest",
              "name": "Ultrasonido",
              "description": "Estudios de ultrasonografía diagnóstica para adultos y pediatría"
            },
            {
              "@type": "MedicalTest", 
              "name": "Ecografía",
              "description": "Estudios ecográficos especializados incluyendo embarazo y ginecología"
            },
            {
              "@type": "MedicalTest",
              "name": "Ecocardiograma", 
              "description": "Estudios cardíacos con tecnología avanzada"
            }
          ],
          "hasCertification": [
            {
              "@type": "Certification",
              "name": "Certificación Secretaría de Salud México",
              "issuedBy": "Secretaría de Salud"
            }
          ],
          "isAcceptingNewPatients": "http://schema.org/True",
          "paymentAccepted": ["Cash", "Credit Card"],
          "priceRange": "$$"
        },
        {
          "@type": "LocalBusiness",
          "@id": "https://centromedicogonzalitos.milcoms.org/#business",
          "name": "Centro Médico Gonzalitos",
          "image": "https://centromedicogonzalitos.milcoms.org/img/servicios/logo-cmg-horizontal.png",
          "priceRange": "$$"
        }
      ]
    }
    </script>
    
    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        :root {
            /* Colores principales (recuperados del sitio archivado) */
            --primary-color: #003da2;
            --primary-hover: #1e3a8a;
            --accent-color: #dc2626;
            --accent-hover: #991b1b;
            --text-dark: #191919;
            --text-light: #9CA3AF;
            --bg-light: #f8f9fa;
            --border-color: #dee2e6;
            --success-color: #10b981;
            --phone-color: #666666;
            
            /* Variables mejoradas para footer - Accesibilidad WCAG */
            --footer-text-primary: #1a1a1a;     /* Negro suave, WCAG AAA */
            --footer-text-secondary: #4a5568;  /* Gris medio, WCAG AA */
            --footer-links-hover: #003da2;     /* Azul primario, excelente contraste */
            --footer-bg: #ffffff;               /* Blanco */
            --footer-border: #e5e7eb;           /* Gris muy claro para bordes */
            --layer-bg: #e1e1e1;

            /* Colores complementarios - Corregidos sin duplicación */
            --success-color: #10B981;
            --success-hover: #059669;
            --warning-color: #F59E0B;
            --danger-color: #EF4444;

            /* Colores neutros */
            --text-dark: #191919;
            --text-muted: #6B7280;
            --text-light: #9CA3AF;
            --bg-white: #FFFFFF;
            --bg-light: #F9FAFB;
            --bg-gray: #F3F4F6;
            --bg-dark: #111827;
            --border-color: #E5E7EB;
            --border-light: #F3F4F6;

            /* Colores de éxito */
            --success-color: #10B981;
            --success-bg: #D1FAE5;

            /* Espaciado */
            --spacing-xs: 0.25rem;
            --spacing-sm: 0.5rem;
            --spacing-md: 1rem;
            --spacing-lg: 1.5rem;
            --spacing-xl: 2rem;
            --spacing-2xl: 3rem;

            /* Sombras */
            --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
            --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
            --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
            --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
        }
        
        body {
            font-size: 15px;
        }

        /* Custom Navbar Styles */
        .navbar-custom {
            background: rgba(255, 255, 255, 0.95) !important;
            backdrop-filter: blur(10px) !important;
            -webkit-backdrop-filter: blur(10px) !important;
            border-bottom: 1px solid var(--border-color) !important;
            transition: all 0.3s ease !important;
            z-index: 1030 !important;
        }

        .navbar-custom.scrolled {
            background: rgba(255, 255, 255, 0.98) !important;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1) !important;
        }

        .navbar-custom .navbar-nav .nav-link {
            color: var(--text-dark) !important;
            font-weight: 500;
            transition: color 0.3s ease;
        }

        .navbar-custom .navbar-nav .nav-link:hover {
            color: var(--primary-color) !important;
        }

        .navbar-custom .dropdown-menu {
            background: rgba(255, 255, 255, 0.98) !important;
            backdrop-filter: blur(10px) !important;
            border: 1px solid var(--border-color) !important;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1) !important;
        }

        /* WhatsApp styles moved to component for consistency */

        /* Animaciones */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .fade-in-up {
            animation: fadeInUp 0.6s ease-out;
        }
        
        /* Responsive Design */
        @media (max-width: 1200px) {
            h1 { font-size: 2.25rem; }
            .display-3 { font-size: 2.5rem; }
            .display-5 { font-size: 2.25rem; }
        }

        @media (max-width: 992px) {
            h1 { font-size: 2rem; }
            .display-3 { font-size: 2.25rem; }
            .display-5 { font-size: 2rem; }
        }

        @media (max-width: 768px) {
            .logo-img {
                max-width: 220px;
                max-height: 60px;
            }

            .navbar-brand {
                font-size: 1.2rem;
            }

            h1 { font-size: 1.75rem; }
            h2 { font-size: 1.5rem; }
            h3 { font-size: 1.25rem; }
            .display-3 { font-size: 1.875rem; }
            .display-5 { font-size: 1.5rem; }
            .lead { font-size: 1.125rem; }

            body {
                font-size: 15px;
            }
        }

        @media (max-width: 576px) {
            h1 { font-size: 1.5rem; }
            .display-3 { font-size: 1.75rem; }
            .display-5 { font-size: 1.375rem; }

            .btn-primary, .btn-whatsapp {
                padding: var(--spacing-sm) var(--spacing-lg);
                font-size: 0.95rem;
            }
        }

        /* Skip Link Styles */
        .skip-link {
            position: absolute;
            top: -40px;
            left: 6px;
            background: var(--primary-color);
            color: white;
            padding: 8px;
            text-decoration: none;
            border-radius: 4px;
            z-index: 10000;
            font-weight: 600;
            font-size: 14px;
        }

.skip-link:focus {
            top: 6px;
        }

        /* Mejoras para SEO Local */
        .address-card {
          background: linear-gradient(135deg, var(--bg-light) 0%, #f8fafc 100%);
          padding: 20px;
          border-radius: 12px;
          border-left: 4px solid var(--primary-color);
          margin-bottom: 20px;
          box-shadow: var(--shadow-sm);
        }

        .area-service {
          background: var(--bg-white);
          padding: 15px;
          border-radius: 8px;
          border: 1px solid var(--border-light);
          transition: all 0.3s ease;
          margin-bottom: 15px;
        }

        .area-service:hover {
          border-color: var(--primary-color);
          transform: translateY(-2px);
          box-shadow: var(--shadow-md);
        }

        .local-highlight {
          background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-hover) 100%);
          color: white;
          padding: 20px;
          border-radius: 15px;
          margin: 20px 0;
          box-shadow: var(--shadow-lg);
        }

        .local-highlight h4 {
          color: white !important;
        }

        .schema-rich-result {
          background: #f0f9ff;
          border: 1px solid #bae6fd;
          border-radius: 8px;
          padding: 15px;
          margin: 20px 0;
        }

        .schema-rich-result::before {
          content: '🏥';
          font-size: 1.5rem;
          margin-right: 10px;
        }

        .service-local-badge {
          background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-hover) 100%);
          color: white;
          font-size: 0.75rem;
          text-transform: uppercase;
          font-weight: 600;
          letter-spacing: 0.5px;
          padding: 4px 8px;
          border-radius: 4px;
          display: inline-block;
          margin-bottom: 8px;
        }

        .location-info {
          background: var(--bg-white);
          border: 1px solid var(--border-color);
          border-radius: 12px;
          padding: 20px;
          box-shadow: var(--shadow-sm);
          margin: 20px 0;
        }

        .directions-info ul {
          list-style: none;
          padding-left: 0;
          margin: 0;
        }

        .directions-info li {
          padding: 8px 0;
          border-bottom: 1px solid var(--border-light);
          transition: background-color 0.3s ease;
        }

        .directions-info li:hover {
          background: rgba(0, 61, 162, 0.05);
        }

        .directions-info li:last-child {
          border-bottom: none;
        }

        /* Geo-specific optimizations */
        .geo-tag {
          background: rgba(0, 61, 162, 0.1);
          color: var(--primary-color);
          padding: 4px 8px;
          border-radius: 15px;
          font-size: 0.8rem;
          font-weight: 500;
          display: inline-block;
          margin: 0 4px 8px 0;
        }

        .service-area-indicator {
          background: var(--success-color);
          color: white;
          padding: 2px 6px;
          border-radius: 3px;
          font-size: 0.75rem;
          font-weight: 600;
          margin-left: 8px;
        }

        /* Local business cards for better UX */
        .local-business-card {
          background: var(--bg-white);
          border: 1px solid var(--border-color);
          border-radius: 15px;
          padding: 25px;
          box-shadow: var(--shadow-md);
          transition: all 0.3s ease;
        }

        .local-business-card:hover {
          transform: translateY(-3px);
          box-shadow: var(--shadow-lg);
        }

        /* Breadcrumb navigation for local SEO */
        .breadcrumb-local {
          background: var(--bg-light);
          padding: 10px 0;
          border-bottom: 1px solid var(--border-light);
        }

        .breadcrumb-local .breadcrumb {
          background: transparent;
          padding: 0;
          margin: 0;
        }

        .breadcrumb-local .breadcrumb-item + .breadcrumb-item::before {
          content: "›";
          color: var(--text-muted);
          font-weight: bold;
        }

        /* Rich snippet optimizations */
        .rating-display {
          display: flex;
          align-items: center;
          gap: 5px;
          margin: 10px 0;
        }

        .rating-stars {
          color: var(--warning-color);
          font-size: 1.2rem;
        }

        .rating-number {
          font-weight: bold;
          color: var(--text-dark);
          margin-left: 5px;
        }

        /* Mobile local optimizations */
        @media (max-width: 768px) {
          .address-card {
            padding: 15px;
            margin-bottom: 15px;
          }

          .area-service {
            padding: 12px;
            margin-bottom: 10px;
          }

          .local-highlight {
            padding: 15px;
            margin: 15px 0;
          }

          .service-local-badge {
            font-size: 0.7rem;
            padding: 3px 6px;
          }

          .geo-tag {
            font-size: 0.75rem;
            padding: 3px 6px;
            margin: 0 2px 6px 0;
          }
        }
    </style>
</head>
<body>
    <!-- Alerta de Promociones Activas -->
    {activePromotions.length > 0 && (
        <div class="promotion-alert">
            <div class="container">
                🎉 {activePromotions.length} {activePromotions.length === 1 ? 'oferta especial' : 'ofertas especiales'} disponible{activePromotions.length === 1 ? '' : 's'} - 
                <a href="/promociones" style="color: white; text-decoration: underline; font-weight: bold;">Ver todas</a>
            </div>
        </div>
    )}
    
    <!-- Skip Links for Accessibility -->
    <a href="#main-content" class="skip-link">Skip to main content</a>
    
    <!-- Header Principal Reorganizado -->
    <header class="header-wrapper bg-white border-bottom">
        <div class="container py-3">
            <div class="d-flex justify-content-between align-items-center">
                <!-- Logo -->
                <a href="/" class="logo-link">
                    <img src="/img/servicios/logo-cmg-horizontal.png" alt="CENTRO MÉDICO GONZALITOS" class="logo-img">
                </a>

                <!-- Navegación Desktop -->
                <nav class="desktop-nav d-none d-lg-flex align-items-center gap-4">
                    <!-- Sobre Nosotros integrado -->
                    <a href="/nosotros" class="nav-link-about text-decoration-none fw-bold" style="color: var(--primary-color);">
                        <i class="fas fa-users me-1"></i>Sobre Nosotros
                    </a>

                    <!-- Separador -->
                    <div class="nav-separator">|</div>

                    <!-- Agenda tu cita con WhatsApp -->
                    <div class="cta-whatsapp d-flex align-items-center gap-2 px-3 py-2 rounded-pill" style="background: rgba(40, 167, 69, 0.1); border: 1px solid rgba(40, 167, 69, 0.2);">
                        <i class="fab fa-whatsapp text-success fs-5"></i>
                        <span class="fw-bold" style="color: var(--phone-color); font-size: 0.9rem;">Agenda tu Cita:</span>
                        <WhatsAppButton 
                            variant="header" 
                            text={siteSettings.phone_number}
                            message="Hola, deseo agendar una cita"
                            size="sm"
                            showIcon={false}
                        />
                    </div>
                </nav>

                <!-- Botón móvil único -->
                <button class="navbar-toggler d-lg-none" type="button" 
                        data-bs-toggle="collapse" data-bs-target="#mainNav" 
                        style="border-color: var(--primary-color);"
                        aria-label="Toggle navigation menu"
                        aria-expanded="false"
                        aria-controls="mainNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
            </div>
        </div>
    </header>

     <!-- Navegación Principal Unificada -->
     <nav class="navbar navbar-expand-lg sticky-top shadow-sm navbar-custom">
         <div class="container">
             <div class="collapse navbar-collapse" id="mainNav">
                 <ul class="navbar-nav w-100 justify-content-center">
<li class="nav-item dropdown me-lg-2">
                          <a class="nav-link dropdown-toggle fw-bold" href="#" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Ultrasonido Adulto</a>
                         <ul class="dropdown-menu">
                              {serviciosPorCategoria.adultos.length > 0 ? serviciosPorCategoria.adultos.map(s => (
                                 <li><a class="dropdown-item" href={`/servicios/${s.slug}`}>{s.data.title}</a></li>
                             )) : <li><span class="dropdown-item disabled">Próximamente</span></li>}
                         </ul>
                     </li>

<li class="nav-item dropdown me-lg-2">
                          <a class="nav-link dropdown-toggle fw-bold" href="#" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Ultrasonido de paciente embarazada</a>
                         <ul class="dropdown-menu">
                              {serviciosPorCategoria.embarazadas.length > 0 ? serviciosPorCategoria.embarazadas.map(s => (
                                 <li><a class="dropdown-item" href={`/servicios/${s.slug}`}>{s.data.title}</a></li>
                             )) : <li><span class="dropdown-item disabled">Próximamente</span></li>}
                         </ul>
                     </li>

                      {serviciosPorCategoria.pediatrico.length > 0 && (
<li class="nav-item dropdown me-lg-2">
                               <a class="nav-link dropdown-toggle fw-bold" href="#" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Ultrasonido Pediatrico</a>
                              <ul class="dropdown-menu">
                                   {serviciosPorCategoria.pediatrico.map(s => (
                                      <li><a class="dropdown-item" href={`/servicios/${s.slug}`}>{s.data.title}</a></li>
                                  ))}
                              </ul>
                          </li>
                      )}

<li class="nav-item dropdown me-lg-2">
                           <a class="nav-link dropdown-toggle fw-bold" href="#" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Ginecología</a>
                          <ul class="dropdown-menu">
                              <li><a class="dropdown-item" href="/servicios">Ver todos los servicios ginecológicos</a></li>
                          </ul>
                      </li>

                      <li class="nav-item me-lg-2">
                          <a class="nav-link fw-bold" href="/promociones">Ultrasonido de embarazo 5D</a>
                      </li>

                      <!-- Elementos móviles adicionales -->
                      <li class="nav-item d-lg-none border-top mt-2 pt-2">
                          <a class="nav-link fw-bold" href="/nosotros">
                              <i class="fas fa-users me-1"></i>Sobre Nosotros
                          </a>
                      </li>
<li class="nav-item d-lg-none">
                           <WhatsAppButton 
                               variant="inline" 
                               text={`📱 ${siteSettings.phone_number}`}
                               size="sm"
                           />
                          </a>
                      </li>
                  </ul>
              </div>
          </div>
      </nav>



    <main id="main-content">
        <slot />
    </main>

     <!-- Footer (estilo archivado) -->
      <footer class="footer-wrapper py-5">
         <div class="container">
             <div class="row">
<div class="col-lg-4 py-2">
                      <img src="/img/servicios/logo-cmg-horizontal.png" alt={siteSettings.site_name} class="mb-3" style="max-width: 205px; height: auto;">
                      
                      <!-- Social Media Links -->
                      <div class="social-media-links mb-3">
                          {siteSettings.facebook_url && (
                              <a href={siteSettings.facebook_url} 
                                 target="_blank" 
                                 rel="noopener noreferrer"
                                 class="social-link facebook"
                                 aria-label="Síguenos en Facebook (abre en nueva ventana)">
                                  <i class="fab fa-facebook-f"></i>
                              </a>
                          )}
                          {siteSettings.instagram_url && (
                              <a href={siteSettings.instagram_url} 
                                 target="_blank" 
                                 rel="noopener noreferrer"
                                 class="social-link instagram"
                                 aria-label="Síguenos en Instagram (abre en nueva ventana)">
                                  <i class="fab fa-instagram"></i>
                              </a>
                          )}
                      </div>
                      
                      <p class="small text-muted">Síguenos en nuestras redes sociales</p>
                  </div>

                 <div class="col-lg py-2">
                     <h4 class="h5 mb-3">Servicios</h4>
                      <nav class="footer-links">
                          <div class="mb-2"><a href="/categoria-de-servicio/ecografias-adulto/" class="d-block text-decoration-none">Ultrasonido Adulto</a></div>
                          <div class="mb-2"><a href="/categoria-de-servicio/ecografias-materno/" class="d-block text-decoration-none">Ultrasonido de paciente embarazada</a></div>
                          <div class="mb-2"><a href="/categoria-de-servicio/ginecologia/" class="d-block text-decoration-none">Ultrasonido Ginecología</a></div>
                          <div class="mb-2"><a href="/categoria-de-servicio/eco-5d/" class="d-block text-decoration-none">Ultrasonido de embarazo 5D</a></div>
                          <div class="mb-0"><a href="/servicios" class="d-block text-decoration-none">Ver todos los servicios</a></div>
                      </nav>
                 </div>

                  <div class="col-lg py-2">
                      <h4 class="h5 mb-3">Navegación Rápida</h4>
                     <nav class="footer-links">
                         <div class="mb-2"><a href="/nosotros" class="d-block text-decoration-none">Nosotros</a></div>
                         <div class="mb-2"><a href="/blog" class="d-block text-decoration-none">Blog</a></div>
                         <div class="mb-0"><a href="/contacto" class="d-block text-decoration-none">Contacto</a></div>
                     </nav>
                 </div>

                 <div class="col-lg py-2">
                      <p class="mb-2"><strong>Agenda una Cita por Whatsapp:</strong><br>{siteSettings.phone_number}</p>
                      <p class="mb-2"><strong>Citas Telefónicas:</strong><br>81 1588 3775<br>81 1360 5630<br>81 3259 9030</p>
                      <p class="mb-0"><strong>Dirección:</strong><br>Av. Dr. José Eleuterio González 135,<br>Leones, 64600 Monterrey, N.L.</p>
                 </div>
             </div>
         </div>
     </footer>

     <div class="bg-dark text-light py-3">
         <div class="container">
             <div class="row align-items-center">
                 <div class="col-12 text-center">
                     <p class="mb-0">© {new Date().getFullYear()} <span style="color: var(--primary-color);">CENTRO MÉDICO GONZALITOS</span> Centro de Ultrasonido Monterrey</p>
                      <p class="mb-0"><a href="/aviso-privacidad/" class="text-light text-decoration-none me-2">Aviso de Privacidad</a> | <a href="https://milcoms.org/" class="text-light text-decoration-none" target="_blank" rel="noopener noreferrer">Design by milcoms</a></p>
                 </div>
             </div>
         </div>
     </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Scripts personalizados -->
    <script>
        // Efecto de scroll suave
        document.querySelectorAll('a[href^="#"]').forEach((element: Element) => {
            const anchor = element as HTMLAnchorElement;
            anchor.addEventListener('click', (e: Event) => {
                e.preventDefault();
                const target = document.querySelector(anchor.getAttribute('href') || '');
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth' });
                }
            });
        });
        
        // Efecto mejorado de navbar al hacer scroll
        window.addEventListener('scroll', function() {
            const navbar = document.querySelector('.navbar-custom');
            if (navbar) {
                if (window.scrollY > 50) {
                    navbar.classList.add('scrolled');
                } else {
                    navbar.classList.remove('scrolled');
                }
            }
        });
        
        // Animación de elementos al cargar
        document.addEventListener('DOMContentLoaded', function() {
            const elements = document.querySelectorAll('.servicio-card, .testimonial-card, .promotion-card');
            elements.forEach((el, index) => {
                setTimeout(() => {
                    el.classList.add('fade-in-up');
                }, index * 100);
            });
        });
    </script>
    
    <!-- Mobile Sticky WhatsApp Button -->
    <WhatsAppButton 
        variant="fab" 
        message="Hola, deseo agendar una cita"
    />
</body>
</html>